{
  "name": "Layer",
  "version": "2.0.0",
  "description": "Layer - A tool for transmuting dreams into reality.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "web": "npx serve .",
    "dev": "doppler run --config dev_personal -- node scripts/inject-secrets.js && npx serve .",
    "inject-secrets": "doppler run --config dev_personal -- node scripts/inject-secrets.js",
    "terminal": "node life.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.93.1",
    "blessed": "^0.1.81",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
